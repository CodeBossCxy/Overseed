// TEMP (beta): the brand pricing page currently shows the simplified V2
// (credit costs + beta grant + starter tasks). To restore the original
// full pricing page, change the export below to './v1'.
export { default } from './v2'
